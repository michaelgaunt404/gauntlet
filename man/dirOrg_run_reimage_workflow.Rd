% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dirOrg_run_reimage_workflow.R
\name{dirOrg_run_reimage_workflow}
\alias{dirOrg_run_reimage_workflow}
\title{Run the full scan -> select -> copy (-> delete) reimage workflow}
\usage{
dirOrg_run_reimage_workflow(
  root,
  dest_root = "E:/gaunt_project_reimage",
  level = 1,
  space_buffer_gb = 25,
  overwrite = FALSE,
  delete_after_copy = FALSE
)
}
\arguments{
\item{root}{Character path to the folder to scan.}

\item{dest_root}{Character path to the destination root folder.
Defaults to `"E:/gaunt_project_reimage"`.}

\item{level}{Integer, depth below `root` to scan. Defaults to `1`.}

\item{space_buffer_gb}{Numeric, minimum free space (in GB) to leave on
the destination drive. Defaults to `25`.}

\item{overwrite}{Logical, whether to overwrite existing destination
folders. Defaults to `FALSE`.}

\item{delete_after_copy}{Logical, whether to delete source folders
whose copy was verified (`copy_status == "copied_verified"`) once
copying finishes. Defaults to `FALSE`.}
}
\value{
The copy log `data.table` from [dirOrg_copy_selected_dirs()],
  or `NULL` (invisibly) if the popup selection is cancelled.
}
\description{
Convenience wrapper that chains [dirOrg_get_dir_sizes_at_level()],
[dirOrg_select_dirs_popup()], and [dirOrg_copy_selected_dirs()], with an
optional final step that deletes verified source folders via
[dirOrg_delete_dirs()].
}
\examples{
\dontrun{
copy_log <- dirOrg_run_reimage_workflow(
  root = "C:/Users/GauntM/Documents/030_projects",
  dest_root = "E:/gaunt_project_reimage",
  level = 1,
  space_buffer_gb = 25,
  delete_after_copy = TRUE
)
}
}
